<h1><%= t('view.detail_task') %></h1>
<div class="">
  <div class="">
      title:<%= @task.title %>
  </div>
  <div class="">
      content:<%= @task.content %>
  </div>
  <%= link_to "edit", edit_task_path(@task.id) %>
  <%= link_to "destroy", task_path(@task.id), method: :delete %>
  <%= link_to "back", tasks_path %>
</div>
